import { Box } from "@chakra-ui/react";
import React from "react";

interface Props {}

function FileName(props: Props) {
  const {} = props;

  return (
    <Box marginTop={2} textAlign={"start"}>
      Transactions
    </Box>
  );
}

export default FileName;
